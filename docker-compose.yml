version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: discipline-ai-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - discipline-network

volumes:
  redis-data:
    driver: local

networks:
  discipline-network:
    driver: bridge
